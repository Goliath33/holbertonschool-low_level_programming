#include "main.h"

/**
 * main - check the code
 * @argc: argument value og string to call function
 * @argv: argument count
 * Return: (SUCCESS)
 */

int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);
	return (0);
}
