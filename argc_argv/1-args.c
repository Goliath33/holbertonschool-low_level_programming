#include "main.h"

/**
 * main - check the code
 * @argc: argument value og string to call function
 * @argv: argument count
 * Return: (SUCCESS)
 */

int main(int argc, __attribute__((unused)) char *argv[])
{
	int i;

	for (i = 0; i < argc; i++)
	{
	}

	printf("%d\n", i - 1);

	return (0);
}

