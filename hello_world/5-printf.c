#include <stdio.h>

/**
 * main - call fonction printf
 * Printf - Donne une reponse et ecris le texte
 * Return: (success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
