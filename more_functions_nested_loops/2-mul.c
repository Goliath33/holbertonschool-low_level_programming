#include "main.h"

/**
 * int mul(int a, int b) - verifie si un caractere est majuscule
 * @a An input characte for multiplication
 * @b ''     ''     ''      ''      ''
 * Return: (SUCCESS)
 */

int mul(int a, int b)
{
	return (a * b);
}
