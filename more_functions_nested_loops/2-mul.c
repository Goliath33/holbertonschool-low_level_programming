#include "main.h"

/**
 * _isdigit(int c) - verifie si un caractere est majuscule
 * @c: An input characte
 * Return: 1 if (SUCCESS) and 0 si pas de majuscule
 */

int mul(int a, int b)
{
	return (a * b);
}
