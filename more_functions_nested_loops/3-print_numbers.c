#include "main.h"

/**
 * int mul(int a, int b) - verifie si un caractere est majuscule
 * @num An input character for _putchar and print character
 * Return: (SUCCESS)
 */

void print_numbers(void)
{
	int num;

	for (num = '0'; num <= '9'; num++)

		_putchar(num);

	_putchar('\n');
}
